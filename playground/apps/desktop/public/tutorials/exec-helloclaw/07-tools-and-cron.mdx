---
title: 第七章 工具与定时任务
description: 让龙虾自己找工具干活、定时执行任务
difficulty: intermediate
duration: 15
category: ai-agent
tags: [openclaw, ai, 工具, 定时任务, cron]
course: exec-helloclaw
courseOrder: 7
source: builtin
---

# 第七章 工具与定时任务

学完本章，你的龙虾就能自己找工具干活、定时执行任务了——搜网页、操浏览器、每天早上自动发天气，一个不落。

## 0. 工具 vs 技能

|  | Tools（工具） | Skills（技能） |
|---|---|---|
| **是什么** | OpenClaw 内置的底层能力 | 社区或用户编写的高层指令 |
| **举例** | 执行命令、读写文件、搜索网页 | "每天早上发晨间简报" |
| **谁提供** | OpenClaw 核心代码 | ClawHub 技能市场 / 用户自己编写 |

一句话：**Tools 是手脚，Skills 是招式。**

## 1. 工具配置档（Tool Profiles）

| 配置档 | 能力范围 | 适用场景 |
|--------|---------|---------|
| `full` | 无限制，所有工具可用 | **推荐**——个人电脑全能助手 |
| `coding` | 文件读写、命令执行、记忆、图片分析 | 开发者专用 |
| `messaging` | 消息收发、会话浏览 | 纯聊天机器人 |
| `minimal` | 仅状态查看 | 最小权限 |

<RunButton command="openclaw config get tools.profile" />

<RunButton command="openclaw config set tools.profile full" />

<RunButton command="openclaw gateway restart" />

> 如果龙虾只给建议、不执行操作，多半是配置档停在了旧版默认值 `messaging`——跑一遍上面的命令就好。

> ⚠️ `full` 模式下 OpenClaw 可在你的电脑上执行任意命令。个人电脑上使用没问题，但**不要在生产服务器上使用 `full` 模式**。

## 2. 内置工具一览

不需要逐个记住——龙虾会自动选工具，你只要确保配置档是 `full`。

| 分类 | 工具 | 说明 |
|------|------|------|
| **文件操作** | `read` `write` `edit` `apply_patch` | 读写文件 |
| **命令执行** | `exec` `process` | 运行 Shell 命令 |
| **网页** | `web_search` `web_fetch` | 搜索引擎查询 |
| **浏览器** | `browser` | 完整的浏览器自动化 |
| **消息** | `message` | 跨渠道收发消息 |
| **定时任务** | `cron` | 创建和管理定时任务 |
| **画布** | `canvas` | 驱动配套 App 的画布 |
| **图片/PDF** | `image` `pdf` | 分析图片、解析 PDF |

## 3. 联网搜索

<RunButton command="openclaw configure --section web" />

配置完成后直接在对话里问：

```
今天有什么科技新闻？
帮我搜索"OpenClaw 最新版本"的更新内容
```

支持搜索引擎：Brave / Perplexity / Kimi / Gemini / Grok

## 4. 浏览器自动化

直接说就行：

```
帮我打开百度搜索"今日天气"，截图给我看
打开 https://example.com，找到登录按钮并点击
```

启用浏览器：

<RunButton command="openclaw config set browser.enabled true" />

<RunButton command="openclaw gateway restart" />

## 5. 定时任务（Cron）

最简单的例子——每天早上 8 点发天气预报：

```bash
openclaw cron add --name "天气预报" --cron "0 8 * * *" \
  --message "查询今天的天气，发送给我" \
  --channel "feishu:chat:你的ChatID"
```

三种调度方式：

```bash
# 固定周期：每 30 分钟执行一次
openclaw cron add --name "定期检查" --every 30m \
  --message "检查有没有需要我处理的事情"

# 一次性延时：20 分钟后提醒
openclaw cron add --name "提醒" --at 20m \
  --message "提醒：该休息一下了！"
```

管理任务：

<RunButton command="openclaw cron list" />

<RunButton command='openclaw cron run 天气预报' />

<RunButton command='openclaw cron disable 天气预报' />

<RunButton command='openclaw cron enable 天气预报' />

> Cron 表达式速查：`0 8 * * *` = 每天 8:00 | `0 9 * * 1-5` = 工作日 9:00 | `*/30 * * * *` = 每 30 分钟

## 6. 命令执行（Exec）

`exec` 是龙虾"干活"的核心——安装软件、处理文件、运行脚本都靠它。直接说需求就行：

```
帮我创建一个文件叫 hello.txt，写上今天的日期
用 Python 写一个猜数字小游戏，保存并运行
```

每次执行前龙虾都会请求你确认。

## 7. 工具权限管理

```bash
# 禁止浏览器
openclaw config set tools.deny '["browser"]'

# 只允许文件操作和搜索
openclaw config set tools.allow '["group:fs", "group:web"]'
```

`deny` 优先于 `allow`。

## 常见问题

**龙虾只会聊天，不执行任何操作？**

<RunButton command="openclaw config set tools.profile full" />

<RunButton command="openclaw gateway restart" />

**联网搜索不可用？** 需要先配置搜索引擎 API Key：

<RunButton command="openclaw configure --section web" />

**定时任务没有执行？** 检查 Gateway 是否在运行：

<RunButton command="openclaw status" />

## 完成检查

- [ ] 工具配置档已设为 `full`
- [ ] 联网搜索已配置（可选）
- [ ] 浏览器已启用（可选）
- [ ] 已创建至少一个定时任务（可选）
- [ ] 理解工具权限管理

---

*内容来源：[hello-claw](https://github.com/datawhalechina/hello-claw) | CC BY-NC-SA 4.0*
