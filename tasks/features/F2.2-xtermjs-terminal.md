# F2.2 xterm.js 终端组件

## Description

在前端实现基于 xterm.js 的终端组件，在应用内嵌入终端视图。

## Acceptance Criteria

- [ ] 集成 xterm.js 到 Next.js 项目
- [ ] 终端组件能正确渲染（支持中文字符、ANSI 颜色）
- [ ] 终端支持基本交互（输入、输出、滚动）
- [ ] 终端尺寸可随窗口调整（resize）
- [ ] 终端主题与应用整体风格一致
- [ ] 组件封装为 `<TerminalPanel>` 可复用

## Technical Notes

```bash
pnpm add @xterm/xterm @xterm/addon-fit @xterm/addon-web-links
```

```tsx
// components/terminal/terminal-panel.tsx
'use client';
import { useEffect, useRef } from 'react';
import { Terminal } from '@xterm/xterm';
import { FitAddon } from '@xterm/addon-fit';
import '@xterm/xterm/css/xterm.css';

interface TerminalPanelProps {
  sessionId?: string;
  onReady?: (terminal: Terminal) => void;
}

export function TerminalPanel({ sessionId, onReady }: TerminalPanelProps) {
  const ref = useRef<HTMLDivElement>(null);

  useEffect(() => {
    const term = new Terminal({
      fontSize: 14,
      fontFamily: 'Menlo, Monaco, "Courier New", monospace',
      cursorBlink: true,
      theme: {
        background: '#1e1e2e',
        foreground: '#cdd6f4',
      }
    });

    const fitAddon = new FitAddon();
    term.loadAddon(fitAddon);

    if (ref.current) {
      term.open(ref.current);
      fitAddon.fit();
    }

    onReady?.(term);
    return () => term.dispose();
  }, []);

  return <div ref={ref} className="h-full w-full" />;
}
```

### 注意事项

- xterm.js 需要在客户端渲染（`'use client'`），不支持 SSR
- Next.js App Router 需要使用 `dynamic(() => import(...), { ssr: false })`
- 需要手动引入 xterm.css

## Dependencies

- F1.2 配置 Monorepo 集成

## Estimated Time

3-4 小时

## Difficulty

中
