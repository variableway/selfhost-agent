# Tracing: F1.2-monorepo-integration

## Task Entry (2026-04-07 01:06:41)

- **Task File**: `tasks/features/F1.2-monorepo-integration.md`
- **Task ID**: local-20260407-2e33664f
- **Title**: F1.2 配置 Monorepo 集成
- **Started At**: 2026-04-07 01:06:41
- **Status**: in_progress

### Original Task Content

```markdown
# F1.2 配置 Monorepo 集成

## Description

确保 `apps/desktop` 能正确使用 monorepo 中已有的 `packages/ui`、`packages/utils`、`packages/tsconfig`。

## Acceptance Criteria

- [ ] `apps/desktop` 的 `package.json` 正确引用 `@innate/ui`、`@innate/utils`
- [ ] shadcn/ui 组件能在 desktop 应用中直接使用（Button, Card 等）
- [ ] TypeScript 配置正确继承 `packages/tsconfig`
- [ ] Tailwind CSS 配置正确，样式正常渲染
- [ ] `pnpm build` 在 monorepo 根目录能正确构建所有包

## Technical Notes

```json
// apps/desktop/package.json
{
  "dependencies": {
    "@innate/ui": "workspace:*",
    "@innate/utils": "workspace:*"
  }
}
```

```json
// apps/desktop/tsconfig.json
{
  "extends": "@innate/tsconfig/nextjs.json"
}
```

需要确保 Tailwind CSS 的 content 路径包含 `packages/ui` 的组件文件。

## Dependencies

- F1.1 初始化 Tauri + Next.js 项目

## Estimated Time

1-2 小时

## Difficulty

低
```

### Agent Parsed Content

将 desktop 应用接入 monorepo：添加 @innate/ui, @innate/utils 依赖，继承 tsconfig，配置 Tailwind CSS 路径覆盖 packages/ui，用 shadcn Button 替换原生 button

